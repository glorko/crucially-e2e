# -----------------------------------------------------------------------------
# Crucially E2E — Crux config pointer (NOT a standalone runnable stack file)
# -----------------------------------------------------------------------------
#
# Crux 0.10+ uses the PLAYGROUND schema: `dependencies:` (list with check/start),
# `services:`, `terminal:`. Older single-app examples with only `backend:` and
# `flutter:` blocks are obsolete for this workspace.
#
# What to use
#   1. Clone the full Crucially workspace (parent folder contains crucially-backend,
#      crucially-mobile, and config.yaml).
#   2. From that workspace root:  crux -c config-local.yaml  (E2E / full local stack)
#
# Documented reference (same shape as the root config, with comments):
#   crucially-metadata/docs/crux_workspace_config.yaml
#
# This file is intentionally not a duplicate of the full stack; it prevents
# copying a stale schema into a new repo.
# -----------------------------------------------------------------------------

# (No YAML body — see workspace ../config.yaml and metadata docs above.)
